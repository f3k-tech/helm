apiVersion: v1
kind: Service
metadata:
  name: {{ include "cap-captcha-server.fullname" . }}
  labels:
    {{- include "cap-captcha-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cap-captcha-server.selectorLabels" . | nindent 4 }}
