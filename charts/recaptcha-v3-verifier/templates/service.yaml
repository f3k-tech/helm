apiVersion: v1
kind: Service
metadata:
  name: {{ include "recaptcha-v3-verifier.fullname" . }}
  labels:
    {{- include "recaptcha-v3-verifier.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "recaptcha-v3-verifier.selectorLabels" . | nindent 4 }}
